# Copy to key.properties (ignored by Git) and fill locally.
storePassword=YOUR_STORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
keyAlias=upload
# Absolute path; use forward slashes on Windows.
storeFile=C:/private/gilt-upload.jks
